import React from 'react';
import ReactDOM from 'react-dom/client';
import DerivTradingPlatform from './DerivTradingPlatform';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DerivTradingPlatform />
  </React.StrictMode>
);